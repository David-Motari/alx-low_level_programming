#include <stdio.h>
#include "main.h"
/**
*_isupper - Returns 1 if c is uppercase Returns 0 otherwise
*
*checks for uppercase character.
*Return: 0
*/
int _isupper(int c)
{
return (c >= 'A' && c <= 'Z');
}
